import React from "react";
import Testimonials from "./Testimonials";
import Header from "./Header";

const SocialProof = () => {
    return (
        <div className="SocialProof__Section">
            <div className="container">
                <Header />
                <Testimonials />
            </div>
        </div>
    );
};

export default SocialProof;
